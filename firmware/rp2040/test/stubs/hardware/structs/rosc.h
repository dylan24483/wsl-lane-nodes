/* host-test stub for <hardware/structs/rosc.h> (v1.2.3 R3-5: the mock ROSC
 * register file — rosc_hw->randombit, the boot-nonce entropy source — lives in
 * mock_pico.h so every test TU shares one definition). */
#include "mock_pico.h"
