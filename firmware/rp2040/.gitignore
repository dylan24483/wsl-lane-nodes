# build artifacts
/build/
# FI-1 bench variant build dir (v1.2.2 — NEVER a release artifact)
/build_fi1/
/release/*.uf2
test/*.exe
*.uf2
*.elf
*.bin
*.hex
*.map
*.o

# The exact controlled two-image bundle is source-controlled. All other UF2
# build products remain ignored by the rules above.
!/release/wsl_phase8b_rp2040.uf2
!/release/wsl_phase8b_rp2040_FI1.uf2
